<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pt.brewit.mapper.ProductMapper">

    <!-- 상품 베스트순으로 가져오기 + 페이징-->
    <select id="selectBestProducts" resultType="ProductDTO">
        select * from tb_product where category_id = #{category_id}
        order by liked desc
        limit #{pager.offset}, #{pager.size}
    </select>

    <!-- 서브카테고리 가져오기-->
    <select id="selectProductsBySub" resultType="ProductDTO">
        select * from tb_product where subcategory_id = #{subcategory_id}
        order by liked desc
        limit #{pager.offset}, #{pager.size}
    </select>

    <!--카테고리별 상품 전체 개수 조회 -->
    <select id="countAllProducts" resultType="long">
        select count(*) from tb_product where category_id = #{category_id}
    </select>

    <!--서브카테고리별 상품 전체 개수 조회 -->
    <select id="countProductsBySub" resultType="long">
        select count(*) from tb_product where subcategory_id = #{subcategory_id}
    </select>

    <insert id="insertProduct" useGeneratedKeys="true" keyProperty="productId">
        INSERT INTO
        tb_product
        (seller_id, name, category_id, subcategory_id, info, nation, weight, is_caffeine, stock, attach_name, attach_origin_name , liked, reg_date, regular_price, discount_price, sale_price)
        VALUES
        (#{seller_id}, #{name}, #{category_id}, #{subcategory_id}, #{info}, #{nation}, #{weight}, #{is_caffeine}, #{stock}, #{attach_name}, #{attach_origin_name}, #{liked},  #{reg_date}, #{regular_price}, #{discount_price}, #{sale_price});
    </insert>

</mapper>
